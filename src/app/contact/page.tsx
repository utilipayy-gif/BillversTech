import type { Metadata } from "next";
import { getServices,getSiteSettings } from "@/lib/content-store";
import BillversLeadForm from "../billvers-lead-form";
import { BillversFooter,BillversHeader } from "../site-chrome";
export const dynamic="force-dynamic";
export const metadata:Metadata={title:"Contact Us | BillversTech",description:"Contact BillversTech to discuss a website, application, marketing or hosting requirement."};
export default async function ContactPage(){const services=await getServices(),settings=await getSiteSettings();return <main className="bh-site"><BillversHeader services={services}/><section className="bh-page-hero bh-page-hero-short"><span className="bh-kicker">CONTACT / START A CONVERSATION</span><h1>Tell us what you want to make <em>possible.</em></h1><p>A rough idea is enough. Share the goal and we&apos;ll help identify the right service and next step.</p></section><section className="bh-contact"><div className="bh-section bh-contact-grid"><div className="bh-heading"><span>CONTACT DETAILS</span><h2>Simple, direct and <i>no pressure.</i></h2><p>Send an enquiry through the form or contact us directly.</p><div className="bh-direct"><a href={`tel:${settings.phone.replace(/\s/g,"")}`}><small>CALL / WHATSAPP</small><strong>{settings.phone}</strong></a><a href={`mailto:${settings.email}`}><small>EMAIL</small><strong>{settings.email}</strong></a></div><div className="company-card address-card"><span>ADDRESS</span><p>{settings.address}</p></div></div><BillversLeadForm whatsapp={settings.whatsapp}/></div></section><BillversFooter/></main>}
